import { CornerResizeDirective } from './corner-resize.directive';

describe('CornerResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new CornerResizeDirective();
    expect(directive).toBeTruthy();
  });
});

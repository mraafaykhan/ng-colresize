import { EdgeresizeDirective } from './edgeresize.directive';

describe('EdgeresizeDirective', () => {
  it('should create an instance', () => {
    const directive = new EdgeresizeDirective();
    expect(directive).toBeTruthy();
  });
});
